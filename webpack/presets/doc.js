/**
 * @module Presets
 * @description Preset is a piece of configuration for processing all files of certain type
 */

/**
 * @typedef {Object} Preset
 * @property {RegExp} test pattern for detection which files to process
 * @property {Array} use chain of loaders
 */
